name = "misracorder-share"
main = "share-worker.js"
compatibility_date = "2026-06-01"

# No secrets: all privileged work happens in the share-data Supabase Edge
# Function. The Worker only needs to know where the project lives.
[vars]
SUPABASE_URL = "https://nclwfgyxlocxtylozdte.supabase.co"
